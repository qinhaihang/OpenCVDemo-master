<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.qhh.opencvdemo"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.StatisticFilterActivity"></activity>
        <activity android:name=".activity.AmbiguityActivity" />
        <activity android:name=".activity.AddActivity" />
        <activity
            android:name=".activity.RotationActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"></activity>
        <activity android:name=".activity.BGR2ARGBActivity" />
        <activity android:name=".activity.AddActivity" />
        <activity android:name=".activity.MeanStdDevActivity" />
        <activity android:name=".activity.BrightContrastActivity" />
        <activity android:name=".activity.MeanStdDevActivity" />
        <activity android:name=".activity.MatConvertBitmapActivity" />
        <activity android:name=".activity.SaveMat2SDActivity" />
        <activity android:name=".activity.ReadImageInfoActivity" />
        <activity android:name=".activity.GrayActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
